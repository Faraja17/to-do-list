   <%- include("header") -%>
   
   <div class="box" id="heading">
        <h1> <%= listTitle %> </h1>
    </div>
   
    <div class="box" id="heading">
        <h2> <%= currentDay %> </h2>
    </div>
     
   <div class="box">
        <% for (let i = 0; i < tasks.length; i++) { %>
            <div class="item">
                <input type="checkbox">
                <p> <%= tasks[i] %> </p>
            </div> 
        <% } %>

        <form action="/" class="form-new-task" method="post">
            <input type="text" name="task" class="task" placeholder="New Task" autocomplete="off"> 
            <button name="list" type="submit" value="<%= listTitle %>">+</button> <!-- changing the button name to "list" and value to the dynamic ejs "listTitle" enables reference in the post request so that the right title goes on each page. -->
        </form>
   </div>

   <%- include("footer") -%>